#include <Tracker.h>

/*
    Features:
    - Schedule:
        - slot activities and time
*/



int main() {
    Tracker tracker;

    // tracker.addActivity();
    tracker.showTable();
    // tracker.dropTable();
    // tracker.displayAll();

    return 0;
}